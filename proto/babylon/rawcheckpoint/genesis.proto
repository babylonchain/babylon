syntax = "proto3";
package babylonchain.babylon.rawcheckpoint;

import "gogoproto/gogo.proto";
import "babylon/rawcheckpoint/params.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/babylonchain/babylon/x/rawcheckpoint/types";

// GenesisState defines the rawcheckpoint module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
